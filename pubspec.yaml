name: dio_curl_interceptor
description: "A Flutter package for logging curl with dio interceptor."
version: 0.0.4
homepage: https://www.memozen.online/
repository: https://github.com/venhdev/dio_curl_interceptor

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  dio: ^5.8.0+1
  colored_logger: ^0.0.4
  flutter:
    sdk: flutter

dev_dependencies:
  http_parser: ^4.0.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
